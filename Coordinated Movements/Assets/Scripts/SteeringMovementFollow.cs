using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * Steers towards a leader agent.
 * Note that this position is determined by GetPosition on the leader to follow, not the leader's position itself.
 */
public class SteeringMovementFollow : SteeringMovement
{

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
